// Não usado mais — substituído por Prisma (ver src/lib/prisma.ts). Este
// arquivo não pôde ser removido neste ambiente (restrição do sandbox),
// fica como stub pra não deixar import quebrado em nenhum lugar.
export {};
