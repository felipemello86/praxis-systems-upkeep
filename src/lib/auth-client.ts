// Não usado mais — use os hooks/funções de "next-auth/react" diretamente
// (signIn, signOut, useSession). Este arquivo não pôde ser removido neste
// ambiente (restrição do sandbox), fica como stub.
export {};
