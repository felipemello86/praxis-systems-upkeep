// Não usado mais — o logout agora é feito direto com `signOut()` de
// "next-auth/react" em components/dashboard.tsx. Este arquivo não pôde ser
// removido neste ambiente (restrição do sandbox), fica como stub.
export {};
